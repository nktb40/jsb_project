<div class="page-header">
  <h3>Таблица всех товаров</h3>
</div>
<%= form_tag destroy_multiple_admin_products_path, method: :delete, data: { confirm: 'Are you sure?' } do %>
<p>
<%= link_to "Добавить товар", new_admin_product_path, :class => 'btn btn-success' %>

<%= submit_tag "Удалить выбранные", :class=>"btn btn-danger", :id=>"inputIcon" %>
</p>

<table class="table table-striped">
  <tr>
    <th></th>
    <th>ID</th>
    <th>Код товара</th>
    <th>Наименование</th>
    <th>Изображение</th>
    <th>Стоимость 1шт.</th>
    <th>Статус</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>
  <% @count = 0 %>
<% for product in @products %>
  <% @count += 1 %> 
  <tr>
    <td><%= check_box_tag "product_ids[]", product.id %></td>   
    <td><%= product.id %></td>
    <td><%= product.product_code %></td>
    <td><%= product.name%></td>
    <td><%= product.image_identifier %></td>
    <td><%= product.price%>  руб. </td> 
    <td><%= product.product_status%> </td>
    <td><%= link_to "Изменить", edit_admin_product_path(product)  %></td>
    <td><%= link_to 'Destroy', product, method: :delete %></td>
 
  </tr>
<% end %>
</table>
<% end %>
<%= paginate @products %>